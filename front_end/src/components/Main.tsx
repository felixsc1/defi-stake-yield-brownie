export const Main = () => {
    // Should show  - token values from the wallet
    //              - Get addresses of the different tokens
    //              - Get balance of the user wallet
    // This information (token addresses etc.) is in the brownie config.
    // However, nodejs can only work within the src folder
    // See deploy.py --> python functions sends config over to frontend src folder
}                       // also sends build folder over that contains all the mock addresses (and dapp token which is not in config)